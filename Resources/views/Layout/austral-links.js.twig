class AustralLinks  {
  constructor() {
    this.links = {
      {% if objectKeyLinkNames is defined %}
        {%- for keyLink, KeyLinkName in objectKeyLinkNames -%}
          "{{ keyLink}}": "{{ KeyLinkName }}"{% if loop.last %}{% else %},{% endif %}
        {%- endfor -%}
      {% endif %}
    };
  }
  getLinks() {
    return this.links;
  }
  getLinkByKey(key) {
    if(this.links[key] !== undefined)
    {
      return this.links[key];
    }
    return null;
  }
}
AustralLinks = new AustralLinks();