class AustralLinks  {
  constructor() {
    this.links = {
      {% if nameByKeysLinks is defined %}
        {%- for keyLink, KeyLinkName in nameByKeysLinks -%}
          "{{ keyLink }}": "{{ KeyLinkName }}"{% if loop.last %}{% else %},{% endif %}
        {%- endfor -%}
      {% endif %}
    };
  }
  getLinks() {
    return this.links;
  }
  getLinkByKey(key) {
    if(this.links[key] !== undefined)
    {
      return this.links[key];
    }
    return null;
  }
}
AustralLinks = new AustralLinks();